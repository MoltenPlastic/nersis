#include "base/vector.hpp"

namespace Graphics {
	class Canvas {
		
	}
	class Image {
		
	}
	void circle();
	void clear();
	void draw();
	void line();
	void point();
	void poly(VectorList Points);
	Canvas Canvas(unsigned int width,unsigned int height);
	Framebuffer Framebuffer();
	Image Image(const char* filename);
}
